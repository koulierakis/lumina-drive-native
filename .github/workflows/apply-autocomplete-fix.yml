name: Apply progressive address autocomplete fix

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/apply-autocomplete-fix.yml
      - .github/scripts/apply_autocomplete_fix.py

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Patch autocomplete behavior
        run: python3 .github/scripts/apply_autocomplete_fix.py
      - name: Install dependencies
        run: npm install --legacy-peer-deps
      - name: Validate TypeScript
        run: npm run typecheck
      - name: Validate web export
        run: npx expo export --platform web
      - name: Commit validated fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/index.web.tsx app/index.tsx
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "fix(web): progressive autocomplete and strict city filtering"
          git push origin main
